{ Sample program
  in TINY language -
  test new if statement grammer
}
read x; { input an integer }
if (0 < x) { don't compute if x <= 0 }
  fact := 1;
  repeat
    fact := fact * x;
    x := 3^2 * x % 1 + 2
  until x = 0;
  write fact  { output factorial of x }
else
  fact := 2